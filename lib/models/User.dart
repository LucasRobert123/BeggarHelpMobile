class User {
  String id, name, phone, email, password, district, num, city, uf;

  User({
    this.id,
    this.name,
    this.phone,
    this.email,
    this.password,
    this.district,
    this.num,
    this.city,
    this.uf,
  });
}
